import { autocompleteClasses, makeStyles } from "@mui/material";
import { display, flexbox } from "@mui/system";

export default makeStyles(
    {
container: {
    width:auto,
    display: flexbox
}
    }
);